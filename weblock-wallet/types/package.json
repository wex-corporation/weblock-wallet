{
  "name": "@wefunding-dev/wallet-types",
  "version": "0.1.9",
  "description": "Types package for Weblock Wallet",
  "files": [
    "dist"
  ],
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "lint": "eslint 'src/**/*.{js,jsx,ts,tsx}'",
    "format": "prettier --write 'src/**/*.{js,jsx,ts,tsx,json,md}'"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "eslint": "^8.57.1",
    "prettier": "^3.0.0"
  }
}